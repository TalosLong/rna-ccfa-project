# RNA 结构修正项目：研究计划修订版

日期：2026-09-09
计划版本：RESEARCH_ROADMAP_REVISION_20260909
核对依据：TalosLong/rna-ccfa-project，main `38f2003d63751551617f759596eac3dca6d591bd`。
文档性质：完整的修订安排与仓库同步任务；本文件尚未写入 GitHub，不是已生效的 PHASE2_DTP_V1.1，也不代表后续实验已经执行。

## 1. 目标与论文定位

主目标：研究如何利用稀疏结构证据，纠正已有 RNA 二级结构预测，同时限制对原正确信息的损伤。

拟验证的核心贡献：证据沿哪些结构关系传播值得信任，如何在配对耦合和结构合法性约束下完成有用的修改，以及如何衡量和控制误改风险。

论文策略：先建立一篇具有独立验证和真实证据价值的 RNA 方法论文；若得到实质性的结构化学习、优化或风险决策贡献，再选择 CCF-A 路线。期刊分区按单位认可年度和 JCR／中科院口径另行核对。期刊目标和会议类别都不能充当研究结论。

现阶段不把“更准确的 2D 不一定改善 3D”作为独立的新发现；NuFold 已有直接先例。下游研究应进一步回答模型是否采用修正输入、修改位置是否影响整体折叠，以及采样和排序是否限制收益。

## 2. 保留的事实与重新安排的工作

### 已完成并保留

- Phase I 的数据标准化、误差分类、基线、ERN、CER 和结果整理。
- `R4_GATE_B_FAIL` 与 `CONSERVATIVE_DEV_GATE_FAIL`，不得改写为成功。
- 在 Legacy121 和干净符号证据条件下，证据增加纠错信息，但非直接证据区域同时出现额外收益与原正确配对损失。
- Phase II 文献、数据来源与任务协议的设计成果；“设计冻结”不等于数据已构建、模型已训练或效果已验证。
- Legacy121 仅作 Phase I 历史诊断数据；external77 继续作为锁定的历史独立资产，不能用于 Phase II 选模、调参或可行性判断。

### 本次调整

| 原安排中的问题 | 修订安排 | 原因 |
| --- | --- | --- |
| 最小基线之后立即进入主模型开发 | 先完成结构化基线的开发侧科学验证，再决定主模型 | 软件正确不等于 ADD／REPLACE 有科学收益 |
| 风险保证主要停留在公式和最低样本数 | 提前审计独立簇数、上界松紧和有效覆盖率 | 240 个簇只是零损失附近的理论最低要求 |
| 真实证据靠后启动 | 第一阶段检查来源、许可、实验条件和可配对参考；正式测试仍在锁定后 | 避免最后才发现没有可用真实数据 |
| 跨预测器迁移作为后期补充 | 数据划分时就预留，开发与最终评价清楚分开 | 防止方法只适合已见来源 |
| 论文写作排在全部实验之后 | 每阶段同步图表、失败记录和论证链 | 尽早发现证据缺口 |
| 2D→3D 可能扩大主线工作量 | 在 2D 主线成立后，以有限模型和预算进行可选验证 | 集中资源于核心假设 |

## 3. 科学问题与阶段判据

| 问题 | 必须比较 | 支持结论的最低证据形式 |
| --- | --- | --- |
| RQ1：完整编辑是否必要？ | 删除式与 ADD／DELETE／REPLACE，同评分信息、同证据预算 | 可比较误改水平下有额外净纠错收益，明确修复 FN／错配伙伴 |
| RQ2：结构关系是否指导有效传播？ | 完整方法、证据屏蔽、关联打乱、关系消融、直接／局部规则 | 非局部增益不能仅由容量、证据数量或局部硬规则解释 |
| RQ3：风险控制是否有用？ | 完整方法、无风险控制、普通阈值筛选 | 有用覆盖率下改善风险；形式保证的假设与样本量成立 |
| RQ4：结论能否迁移？ | 四类来源、RNA 家族分组、留一预测器类别 | 独立评价和预先定义的迁移场景支持主张，不能靠总体均值掩盖来源退化 |
| RQ5：真实证据是否有价值？ | 同真实证据下的局部修正、约束重折叠、完整方法 | 冻结处理流程后，收益和伤害均有可追溯验证 |
| 可选 RQ6：2D 修正何时改善 3D？ | 原始、局部修正、完整修正、真实 SS 诊断条件 | 固定采样预算与结构选择规则下，配对分析支持具体下游结论 |

本表定义研究判据，不新增已生效的数值门槛。效应量阈值、非劣界、最低覆盖率、统计方法及主要比较必须在对应评估前给出科学理由并冻结；不得事后挑选能通过的指标。

## 4. 总体排期

按一名主要研究者、需要兼顾现有任务估算，总工作预算约 12–16 周。下面是里程碑窗口，可部分重叠；不是日历截止日期或成功承诺。算力排队、许可证、数据可得性与审计结果会影响工期。第一周实际盘点后再落到日期。

| 阶段 | 建议窗口 | 核心产出 | 继续条件 | 未满足时的处理 |
| --- | --- | --- | --- | --- |
| M0 计划与可行性审计 | 第 1 周 | 计划同步、风险样本预算、规则冲突清单、数据来源清单 | 后续工作有可执行协议与数据获取路径 | 先完成前瞻性协议修订，不进入大规模训练 |
| M1 数据和最小软件底座 | 第 2–3 周 | Development-v2、五类分区、四类预测器预检、精确解码与编辑核算 | 来源可复现、分区隔离、软件正确 | 处理数据／工具问题；不静默删掉失败来源 |
| M2 结构化基线科学验证 | 第 4–5 周 | 匹配条件下的删除式与完整编辑比较 | ADD／REPLACE 有额外实用价值 | 收缩为删除式／局部修正研究，不自动扩大模型 |
| M3 简单学习式传播 | 第 6–8 周 | 一个主方法、匹配对照、传播机制实验 | 优于最强匹配简单基线，非局部贡献可归因 | 保留负结果，检验问题定义，不靠堆模块救分 |
| M4 开发侧稳健性与策略锁定 | 第 9–10 周 | 噪声、来源迁移开发分析、风险方案审计、最终策略锁定 | 实用收益、误改与覆盖率达到事先规定标准 | 限定适用范围；无效的形式保证不写入主张 |
| M5 一次性确认 | 第 11–12 周 | 正式校准、封存开发评估、独立数据、锁定的真实证据评价 | 结论在预定条件下成立 | 如实报告不成立的主张，不用确认集救调参 |
| M6 写作与可选下游验证 | 第 13–16 周 | 论文、复现包；可选有限 3D 实验 | 论文主张均有证据，结果局限清楚 | 缩小论文范围或投稿定位 |

M0–M4 的“开发分析”只使用 TRAIN／MODEL_SELECTION 等被明确允许的角色。SCORE_CALIBRATION、RISK_CALIBRATION 和 DEVELOPMENT_ASSESSMENT 的角色不能因为排期变化而被挪作选模数据。最终校准和评估必须遵循锁定顺序。

## 5. M0：第一周完成什么

### 5.1 独立数据簇与风险预算

首先审计可合法获得的数据版本、原始记录数量、家族／同源关系，以及分成五种角色后的风险校准簇预算。仅有原始 RNA 数量不能证明独立簇数充足。

分两步报告：来源元数据可以给出上限或初步估计；实际聚类后的精确数量由后续授权的数据构建任务产生。不能把估计填成实测计数。

当前冻结参数为六个策略、alpha=0.10、delta=0.05：

`U_k = min(1, mean_loss_k + sqrt(log(6/0.05)/(2*n)))`。

| 风险校准独立簇数 | 上界附加量 | 能通过时的最大经验平均损失 |
| ---: | ---: | ---: |
| 240 | 0.099870 | 0.000130 |
| 500 | 0.069192 | 0.030808 |
| 1,000 | 0.048926 | 0.051074 |

这些是公式计算，不是实验表现。需要评估“可用簇数 × 可达到误改率 × 修改覆盖率”是否支持非平凡策略。配对、种子和证据重复不能增加独立簇数。

若不足：在任何相关结果暴露前，研究有明确适用条件的更紧风险界，或将主路线降为经验性选择风险评价。不能先看失败结果再换界，也不能靠放宽 alpha 或移用测试数据获得通过。保留当前冻结方案作为历史版本。

### 5.2 必须先澄清的协议问题

1. **硬证据与 ABSTAIN：** 如果 E0 要求添加一个原结构不存在的配对，恢复 S0 会违反硬证据。需用反例明确：哪些直接修改必做、哪些传播修改可拒绝、完全回退如何标记失败，以及匹配无证据对照如何定义。不能同时宣称“所有硬证据始终满足”和“任意时候完整恢复 S0”。
2. **结构化目标与精确解码：** 明确评分可分解的形式及 DP 能精确求解的范围。图网络生成上下文分数不意味着任意高阶目标均可由同一 DP 精确优化。
3. **风险指标：** 区分原 TP 保留率、每 RNA／簇有害编辑比例、编辑覆盖率和被修改 RNA 比例。不修改 RNA 的零损失会影响总体平均，必须同时报告覆盖率。
4. **统计假设：** 普通 Hoeffding 需要适当的独立抽样条件；不能仅凭“做了家族聚类”或“可交换”自动宣称满足。新家族／新预测器分布变化不被同分布边际保证覆盖。
5. **最小基线与科学门槛：** 现有 P3 仅允许合成样例上的软件底座，不允许直接开展生物学性能实验。M2 必须有单独明确的评估协议和执行范围。

这一步产出问题清单和解决方案；本计划不私自替换已冻结的 E0、风险界、解码器或数据角色。

### 5.3 真实证据来源预检

优先核查 SHAPE 或 DMS 中一种可完整验证的模态。记录数据来源、使用许可、测量条件、原始／归一化数值、重复、缺失、序列版本、可匹配结构和样本独立性。实验环境与参考结构不同可能产生真实构象差异，不能全部算作预测错误。

此时只做来源和数据需求审计；不读取锁定真实测试集的值或标签，不产生真实证据性能结果。预留真实证据开发与确认角色，先确定分配办法再使用具体数据。

## 6. M1–M2：先验证结构化编辑的必要性

### 数据与来源

沿用当前主面板：RNAfold、MXfold2、UFold、RiNALMo。依次核对版本、权重哈希、环境、许可证、输入输出和重复运行一致性。可先用合成输入打通两类接口，但正式主面板比较不能只报告跑得顺利或结果较好的来源。

Development-v2 延续冻结的 RNA3DB-2D 实验核心、80/80 同源与家族／来源连通簇隔离、五种角色及时间审计。准确计数、可执行哈希与分区构建仍是待完成工作。比较注释不能悄然流入实验参考确认角色。

在看结果前固定评估单位、证据预算、种子数量、主比较和排除规则。新增 Independent-v2 采用独立封存流程，external77 不承担新数据可行性审计角色。

### 最小软件验证

先验证合法候选集、短序列穷举对照、DP 精确性、编辑提取、REPLACE 耦合、组件回退、硬约束一致性及确定性。软件报告不称作方法有效性结果。

### 最小科学比较

| 对照 | 作用 |
| --- | --- |
| 原始 S0 | 纠错和保留的零修改锚点 |
| 直接／局部证据修正 | 确定硬证据本身的收益 |
| 全局证据约束重折叠 | 回答“为什么不重新折叠” |
| 删除式结构化修正 | 隔离添加和替换的作用 |
| 完整最小编辑，无学习式传播 | 检验 ADD／REPLACE 的额外价值 |

比较使用相同 RNA、来源、证据及适用候选信息；行动空间差异明确报告。对原本不支持某类输入或动作的基线，说明不适用及适配规则，不能硬凑不等价比较。

报告 F1、原 TP 损失、原 FP 移除、新增 TP／FP、正确／有害 ADD／DELETE／REPLACE、净收益、覆盖率、耗时，以及按 RNA／簇的配对差异。展示风险–收益关系；不能事后在测试曲线上挑点宣称胜利。

M2 的开发侧收益门槛和置信区间规则在运行前冻结，使用允许的开发角色验证。成功才支持 M3 的投入；不成功时回到更窄的局部／删除式问题。最终确认角色不参与这一决定。

## 7. M3：一个主方法和必要消融

先实现一个简单评分器，检查其是否超越最强非学习基线。再根据明确的剩余误差，论证结构关系模型的必要性；不预先承诺主模型必须是 GNN 或 Graph Transformer。

核心机制只聚焦：证据来源与路径、传播可用性、与原预测的冲突，以及候选编辑对耦合结构的影响。沿用已冻结的关系与路径边界；变更先修订协议。

必须保留五类科学对照：证据屏蔽、证据关联打乱、关系消融、直接／局部修正、相同评分器的普通阈值／无风险控制。打乱若因分组过细成为恒等变换，需报告可实际打乱比例，不得当作有效消融。

主结论关注 PROPAGATED 区域是否有可归因的净收益，不以 DIRECT 的易得收益替代。所有源的结果均报告；不得根据 Phase I 的事后分层硬编码一套救分规则。

## 8. M4–M5：从开发到一次性确认

### 开发侧

在允许的开发角色上检查 0%、5%、10% 的主要噪声设置；20%、30% 作为已定义的压力测试。噪声预处理、矛盾、缺失和证据可信度的语义需要单独冻结。尤其要说明 E0 的硬约束如何过渡到可能错误的 E1／E2，不能简单翻标签后继续当绝对真值。

根据开发结果完成模型、动作、证据处理、最低收益、风险目标、覆盖率要求和全部比较的锁定。真实证据若用于适配，只能进入明确的开发角色。

### 锁定后的顺序

1. 锁定模型、预处理、候选与解码器、完整策略族以及评价计划。
2. 仅在 SCORE_CALIBRATION 拟合已指定的概率校准器。
3. 仅在 RISK_CALIBRATION 执行固定风险策略选择。
4. 对封存的 DEVELOPMENT_ASSESSMENT 做一次正式评估。
5. 按封存协议对 Independent-v2 与真实证据确认集做一次评价。

若形式风险控制不满足假设或无法选出非平凡策略，就报告该结果。不得为了让独立集表现更好而重新校准或移动阈值。来源留出和分布迁移的保证范围须另行论证；没有证明时只报告经验结果。

主报告包括按簇配对统计、效应量与置信区间、来源／长度／家族分组、改善／持平／变差比例、完整编辑核算、风险–覆盖率以及计算成本。重要主比较预先确定，多重比较处理提前规定。

独立结果不成立时，收缩泛化结论。后续新假设需要新的版本与未被使用的确认数据，不能重复消费原确认集。

## 9. 可选 2D→3D 与写作

仅在 2D 主线成立且资源允许时开展。优先选择 1–2 个明确支持外部 SS 的模型，先做输入兼容性预检。候选可考虑 trRosettaRNA2 或 RNAbpFlow；这不是本计划对具体版本可运行性的确认。

同一 RNA 比较原始 SS、局部修正 SS、完整修正 SS、真实 SS 诊断条件；固定模型版本、MSA、采样量、随机方案、后处理及无真值选择规则。采样最佳质量与实际选择质量分别报告。

除 TM-score、RMSD、lDDT 外，检查输入配对是否被 3D 输出实现；在预先定义的结构区域比较收益，避免只挑成功结构讲故事。F1 相近不意味着错误位置相同，结构关键性是待验证假设。

写作从 M0 开始，保留一篇主论文的连贯故事：Phase I 诊断→结构化方法→证据归因→风险和泛化→真实证据。是否单独形成 Phase I 论文，在贡献边界足够清楚时再决定，避免把同一贡献拆成重复稿件。

拟定主图：任务与动作空间；基线风险–收益；传播归因；编辑风险–覆盖率；独立与真实证据；可选下游实例。实验结论不能被预设图意替代。

## 10. 每周检查与资源安排

以约 70% 精力推进当前关键路径、20% 整理数据／统计／论文、10% 阅读或支线为初始预算；按实际工作调整。假结和大规模 3D 重跑保留在支线，不挤占关键可行性验证。

每周固定回答五个问题：本周新增了什么证据？哪个假设被支持或否定？还有什么阻塞？下周唯一主要任务是什么？本周产物能否复现？

每个完整里程碑同步 STATUS、TODO、决策记录、必要的 claim–evidence map，检查 diff 后提交并 push。进度文件只记录实际完成项。等待下载或计算期间可整理现有结果，但不提前打开锁定数据。

## 11. 交给服务器 Codex 的下一条任务

任务名：`REVISE_PHASE2_ROADMAP_AND_AUDIT_FEASIBILITY`。

以下整段可作为下一次任务输入，并同时提供本修订文件：

```text
继续 TalosLong/rna-ccfa-project。根据随附的
RNA_Research_Plan_Revised_2026-09-09.md，完成研究计划重排与可行性审计。

本次授权范围：修改计划文档，读取当前状态及公开来源元数据，完成无需
实验性能结果的数学／协议审计。目标是把下一步落实为可执行任务，不是
重跑 Phase I 或开始 Phase II 主模型训练。

一、先核对当前分支、HEAD、origin/main、工作区修改，并读取 AGENTS.md
及其要求的权威文档，重点检查 STATUS.md、tasks/TODO.md、
plan/research_plan.md、plan/timeline.md 和 Phase II 的数据、风险、
解码器、最小基线与 freeze manifest。如果仓库晚于
38f2003d63751551617f759596eac3dca6d591bd，以实际完成证据为准，
吸收新增进度；不要回退已完成工作。保护无关用户改动。

二、同步修订路线：
M0 可行性与协议审计；M1 数据和最小软件；M2 结构化基线科学验证；
M3 简单学习式传播；M4 开发稳健性与策略锁定；M5 一次性确认；
M6 写作与可选 3D。每阶段写明目标、输入角色、产出、继续条件、
失败处理和预计工作窗口。区分 P3 软件正确性与 M2 生物学收益。

三、完成本次可做的审计：
1. 根据冻结公式计算 n=240/500/1000 时的风险上界余量，解释独立簇
   数量与非零修改覆盖率的关系。核查数据来源元数据可提供的预算；
   精确聚类数量尚不可得时标 UNKNOWN，安排后续数据构建，不能编造。
2. 用合成反例检查 E0 硬证据与恢复 S0／all-abstain 的逻辑冲突，
   列出可行语义方案和需要修订的文件，不擅自选择并伪装成旧协议。
3. 审核目标可分解性与精确 DP 的适用条件、风险抽样假设、
   TP preservation／HarmRate／coverage 的区别。
4. 以来源元数据审计一种 SHAPE 或 DMS 路线：许可、条件、序列和
   参考结构可匹配性、缺失、重复，以及开发／确认分离的需求。
   不读取锁定测试值，不生成证据或运行性能评价。

四、文件交付：
- 更新 plan/research_plan.md、plan/timeline.md、STATUS.md、tasks/TODO.md；
- 新增 docs/phase2_roadmap_revision_20260909.md；
- 新增 docs/phase2_feasibility_audit.md，区分实测、公式推导、估计和 UNKNOWN；
- 在 docs/reboot_v2_decisions.md 追加本次计划决策；
- AGENTS.md 仅在下一任务引用需要同步时作最小修改，不改写授权或研究约束；
- claim map 仅在需要澄清状态时修改，不新增实验成功主张。

五、协议边界：
保留 Phase I 所有结果和失败状态；Legacy121 仍为历史诊断数据；
external77 保持锁定。现有 PHASE2_DTP_V1.0 和 freeze manifest 不静默覆盖。
路线重排不等于风险参数、证据语义或数据划分已经修订生效。
需要语义修订时，写明前瞻性 amendment 的旧条款、新候选、理由、
受影响文件、待解决条件；尚未解决的内容不能标 FROZEN。
不运行训练、预测、E0/E1/E2 生成、Development-v2 性能、独立测试或 3D。

六、验证和同步：
检查当前下一任务引用一致；历史下一任务保留时明确标历史。
核对冻结文件哈希未变，确认未新增结果、未读锁定数据。
审查 git diff 与 git status，完成一致的文档提交并 push 到 origin/main。
不 force-push；若远端变化，先安全整合并再次检查。push 受阻时保留提交，
报告具体原因，不声称已经同步。

最终用中文报告：计划改了什么；审计确认了什么／哪些仍未知；
最关键阻塞；下一条有明确范围的任务；commit SHA 与 push 状态。
完成本任务后停止，不自动启动主模型或独立评价。
```

## 12. 依据

- 当前状态：[STATUS.md](https://github.com/TalosLong/rna-ccfa-project/blob/38f2003d63751551617f759596eac3dca6d591bd/STATUS.md)
- 现有研究计划：[research_plan.md](https://github.com/TalosLong/rna-ccfa-project/blob/38f2003d63751551617f759596eac3dca6d591bd/plan/research_plan.md)
- 现有时间线：[timeline.md](https://github.com/TalosLong/rna-ccfa-project/blob/38f2003d63751551617f759596eac3dca6d591bd/plan/timeline.md)
- 风险协议：[phase2_risk_control_design.md](https://github.com/TalosLong/rna-ccfa-project/blob/38f2003d63751551617f759596eac3dca6d591bd/docs/phase2_risk_control_design.md)
- 本轮前序评估已阅读的论文：NuFold（2025，Fig. 3）、trRosettaRNA2（2026）、RNAbpFlow（2026）。此计划未重新开展文献系统检索，也不声称上述模型已经完成本地部署。
