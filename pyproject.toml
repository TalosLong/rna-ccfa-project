[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rna-ccfa"
version = "0.1.0"
description = "Canonical RNA secondary-structure parsing and validation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
